---
layout: core/base
---

<section class="section">
    <h1>{{ title }}</h1>
    {% if subtitle %}
        <h2>{{ subtitle }}</h2>
    {% endif %}
    {% if date %}
        <em>{{ date | verboseDate }}</em>
    {% endif %}
</section>

<section class="section">
    {{ content | safe }}
</section>